# cli-app
Small CLI application that stores data points in file.

Usage:
./Main insert <metric> <value> - inserts datapoint created now to metric
./Main query <metric> [--last time(s/m/h)] - queries for saved datapoint in metric
./Main clean - cleans file
